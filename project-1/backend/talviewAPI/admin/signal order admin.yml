info:
  name: signal order admin
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:4002/api/orders/{id}/signal/{signal}
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
