info:
  name: login-anaka
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4002/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "anaka@talview.com",
        "password": "Anaka123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
