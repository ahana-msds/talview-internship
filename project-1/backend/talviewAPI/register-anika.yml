info:
  name: register-anika
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4002/api/auth/register
  body:
    type: json
    data: |
      {
        "email": "anika@talview.com",
        "password": "Anika123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
