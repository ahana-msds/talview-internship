info:
  name: register-admin
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4002/api/auth/register
  body:
    type: json
    data: |
      {
        "email": "admin@talview.com",
        "password": "Admin@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
