info:
  name: login-ahana
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4002/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "ahana@talview.com",
        "password": "Ahana123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
