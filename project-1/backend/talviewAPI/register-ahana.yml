info:
  name: register-ahana
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4002/api/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
