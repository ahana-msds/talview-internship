import styles from '../Navbar.module.css';

/**
 * NavbarLogo: Displays the application's logo/title in the navigation bar.
 */
export const NavbarLogo = () => {
    return (
        <div className={styles.logo}>
            Project App
        </div>
    );
};
