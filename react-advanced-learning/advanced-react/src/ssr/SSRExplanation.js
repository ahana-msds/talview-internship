export default function SSRExplanation() {
    return (
        <p>
            ssr means rendering react on the server before sending html to browser.
            improves seo and first load performance.
        </p>
    );
}
